*.log
.DS_Store
dist/
node_modules/
playwright-report/
test-results/

# Working notes. Internal context, not documentation, so it stays local.
STATE.md

# publish artifacts
*.tgz
# The site imports the library from beside itself; `npm run site` links it, the deploy copies it.
site/index.js
